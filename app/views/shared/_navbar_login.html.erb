<%= button_to 'Login', 'auth/auth0', method: :post, class: "btn btn-primary" unless logged_in? %>
<% if logged_in? %>
  Hello <%= username %>
  <%= button_to 'Logout', auth0_logout_path, method: :get, class: "btn btn-primary" %>
<% end %>
