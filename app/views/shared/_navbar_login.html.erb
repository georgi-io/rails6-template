<%= button_to 'Login', 'auth/auth0', method: :post, class: "btn btn-primary" unless logged_in? %>
<% if logged_in? %>
  <%= username %>
  <%= link_to auth0_logout_url do %>
    <%= image_tag gravatar_image, class: 'gravatar' %>
  <% end %>
<% end %>
